#include "Jankpch.h"
#include "Colour.h"

namespace Jank
{
	const glm::vec4 Colour::Red = glm::vec4(1.0f, 0.0f, 0.0f, 1.0f);
	const glm::vec4 Colour::Green = glm::vec4(0.0f, 1.0f, 0.0f, 1.0f);
	const glm::vec4 Colour::Blue = glm::vec4(0.0f, 0.0f, 1.0f, 1.0f);
}